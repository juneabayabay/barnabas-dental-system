# Frontend environment (production)
# Copy to .env.local for local overrides.

# Full API base URL including /api suffix (required on Vercel)
VITE_API_URL=https://your-render-api.onrender.com/api

# Local development: leave unset to use Vite proxy (/api → http://127.0.0.1:8000)
