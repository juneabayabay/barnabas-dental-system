# Production API base URL (must include /api suffix).
#
# Vercel + Render (separate hosts) — set in Vercel project env:
VITE_API_URL=https://your-backend.onrender.com/api
#
# Same-origin reverse proxy (optional; requires proxying /api to Django):
# VITE_API_URL=/api
