# Production API base URL (include /api suffix).
# Option A — same origin reverse proxy: serve dist/ and proxy /api to Django
VITE_API_URL=/api

# Option B — separate API host (configure Django CORS for your frontend origin)
# VITE_API_URL=https://api.yourclinic.com/api
